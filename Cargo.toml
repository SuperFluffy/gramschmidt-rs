[package]
name = "gram-schmidt"
version = "0.2.0"
authors = ["Richard Janis Goldschmidt <janis.beckert@gmail.com>"]

[dependencies]
blas = "0.15"
ndarray = "0.10"

[dependencies.ndarray-parallel]
version = "0.5"
optional = true

[dev-dependencies]
ndarray-rand = "0.6"
rand = "0.3"

[features]
parallel = ["ndarray-parallel"]

# A feature used for testing and benching.
test = ["parallel"]
